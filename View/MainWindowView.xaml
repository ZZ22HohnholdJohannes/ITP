<Window x:Class="Reserve_iT.View.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:pg="clr-namespace:Reserve_iT.View"
        Title="Hotel Management" Height="550" Width="800">

  <Grid Background="Black">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- Top Right Panel -->
    <StackPanel Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10">
      <Border Background="#333" Width="120" Height="50" CornerRadius="5" Padding="5" VerticalAlignment="Top">
        <Image Source="pack://application:,,,/Images/Logo_Dark.png" Height="40" HorizontalAlignment="Center"/>
      </Border>

      <Button Content="zurück" FontSize="24" Command="{Binding GoBackCommand}"
                    Background="Transparent" Foreground="White" BorderBrush="Transparent"
                    Padding="10" Cursor="Hand" VerticalAlignment="Top"/>
    </StackPanel>

    <!-- Content Area -->
    <Frame Grid.Row="1" x:Name="MainFrame" NavigationUIVisibility="Hidden" />
  </Grid>
</Window>
